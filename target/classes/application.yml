server:
  port: 9000

spring:

  datasource:
    url: jdbc:mysql://localhost:3306/product_management?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
